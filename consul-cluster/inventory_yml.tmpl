all:
  children:
    consul_instances:
      hosts:
%{~ for index, public_ip in content }
        consul${index}:
          ansible_host: ${public_ip}
%{~ endfor }
  vars:
    ansible_user:  ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
