all:
  children:
    prometheus:
      hosts:
        "${hostname}":
          ansible_host: "${public_ip}"
  vars:
    grafana_url: "http://${hostname}.${public_ip}.${domain}"
    ansible_user:  ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
